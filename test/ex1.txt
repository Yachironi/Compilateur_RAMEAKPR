
class Test()
is{
  var static cpt : Integer;
  var name : String := "x" & cpt.toString(); 
  var x : Integer;
  var y : Integer;

  def print() returns Void {
      /* Exemples avec l'operateur & de concatenation de chaines */
      ( "[" & name & "]" ).print();
      ( "= (" & x.toString() & ", " & y.toString() & ")" ).println();
  }
}
{
  var x : Integer;
  var str : String;
  is
  ("a" & "b" & "ajbdqhsfvdgsv").println();
  1+2;
}
